import AuthForm from '../components/AuthForm';

const Home = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <AuthForm />
    </div>
  );
};

export default Home;
